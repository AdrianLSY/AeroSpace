/.idea
/.debug
/.release
/.shell-completion
/.site
/.man
/Gemfile.lock
# IDK, AppCode randomly creates this EMPTY file. I have no idea what this is
/default.profraw
.DS_Store
/xcode/.xcode-build
# Swift package manager
/.build
/.swiftpm
/.vscode
# External dependencies
/.deps

# For whatever local files that developers might want to keep there (I personally keep a separate `generated-html` git worktree here)
/.local

# XCode User settings
xcuserdata/
xcshareddata/

/openspec

# graphify knowledge graph. The graph itself is committed so it is queryable
# from a fresh clone. These are not: cache/ is a regenerable cache keyed to the
# graphify version, *.bak are throwaway snapshots, and .graphify_python /
# .graphify_root store absolute paths that are wrong on any other machine.
/graphify-out/cache/
/graphify-out/*.bak
/graphify-out/.graphify_python
/graphify-out/.graphify_root
